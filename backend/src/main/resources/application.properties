spring.application.name=backend

server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/certificates_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

app.jwt.secret=replace-with-a-very-long-secret-key-for-jwt-signing-at-least-32-bytes
app.jwt.expiration-ms=86400000

app.storage.pdf-dir=generated/pdfs
app.storage.qr-dir=generated/qrcodes
app.upload.dir=uploads

# OAuth2 LinkedIn placeholders (commented to avoid startup errors without credentials)
# Toggle OAuth2 in SecurityConfig with app.oauth2.enabled.
app.oauth2.enabled=false
# Uncomment and set LINKEDIN_CLIENT_ID / LINKEDIN_CLIENT_SECRET to enable.
# app.oauth2.redirect-uri=http://localhost:4200/login
# spring.security.oauth2.client.registration.linkedin.client-id=${LINKEDIN_CLIENT_ID:}
# spring.security.oauth2.client.registration.linkedin.client-secret=${LINKEDIN_CLIENT_SECRET:}
# spring.security.oauth2.client.registration.linkedin.scope=openid,profile,email
# spring.security.oauth2.client.registration.linkedin.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
# spring.security.oauth2.client.registration.linkedin.client-name=LinkedIn
# spring.security.oauth2.client.provider.linkedin.authorization-uri=https://www.linkedin.com/oauth/v2/authorization
# spring.security.oauth2.client.provider.linkedin.token-uri=https://www.linkedin.com/oauth/v2/accessToken
# spring.security.oauth2.client.provider.linkedin.user-info-uri=https://api.linkedin.com/v2/userinfo
# spring.security.oauth2.client.provider.linkedin.user-name-attribute=sub

# Gemini quiz generation. If no key is configured, backend uses local fallback questions.
app.gemini.enabled=true
app.gemini.api-key=AIzaSyBNn3BVrDz9KZmDk_KnFSvEPlfd5OLzb8c
app.gemini.model=gemini-2.5-flash

spring.batch.jdbc.initialize-schema=always
spring.batch.job.enabled=false

# Mail configuration (Gmail app password)
# To enable: set APP_MAIL_ENABLED=true and provide your Gmail credentials
# 1) Enable 2FA on your Google account
# 2) Generate an App Password: myaccount.google.com/apppasswords
# 3) Replace the values below
app.mail.enabled=true
app.mail.from=${APP_MAIL_FROM:${spring.mail.username:}}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME:rayen.9b7@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:onpo bbcz pynd fdwd}
spring.mail.protocol=smtp
spring.mail.default-encoding=UTF-8
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
